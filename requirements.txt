# Job Hunting MCP Server Dependencies

# FastMCP framework (includes HTTP transport)
fastmcp>=0.2.0

# Database
psycopg2-binary>=2.9.9

# Embeddings
sentence-transformers>=2.2.0

# Databricks SDK
databricks-sdk>=0.30.0

# Web server (included with FastMCP but explicit for clarity)
starlette>=0.27.0
uvicorn>=0.23.0
